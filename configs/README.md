# Configs

Configuration files for reproducible preprocessing, scoring, baseline
generation, plotting, or analysis runs.

TODO: Decide config format, for example YAML, TOML, or JSON.

Suggested future structure:

```text
configs/
|-- preprocessing/
|-- scoring/
|-- baselines/
`-- plotting/
```

TODO: Add real config examples only after the expected fields are decided.
