# Scripts

Small command-line wrappers for repeated project workflows.

Use `src/` for importable project code. Use `scripts/` for thin orchestration
commands that call project code with chosen configs.

TODO: Add scripts only when a workflow is repeated enough to deserve one.

Possible future scripts:

- `run_preprocessing.sh`
- `build_vocabularies.sh`
- `run_scoring.sh`
- `validate_outputs.sh`
- `make_figures.sh`
